Basic Password Strength Checker (C)

Project Overview

This project implements a basic password strength checker using the C programming language. The program validates a user’s password based on simple security rules.

Features

Checks for minimum password length (8 characters)

Ensures at least one digit is present

Ensures at least one uppercase letter is present

Displays appropriate messages based on validation results


Requirements

C Compiler (GCC or any standard C compiler)

Standard C libraries


How to Compile

Use the following command to compile the program:

gcc password_checker.c -o password_checker

How to Run

After compilation, run the program using:

./password_checker

Sample Output

Enter your password: Test
